»ндексы Ц это особые структуры данных, которые хран€т малую часть данных коллекции в форме, удобной дл€ поиска. »ндекс хранит значение указанного пол€ или набора полей, причем пол€ упор€дочены. 

»ндексы обеспечивают эффективное выполнение запросов в MongoDB. Ѕез индексов MongoDB выполн€ет сканирование коллекции, то есть просматривает каждый документ в коллекции, чтобы вы€вить те, которые соответствуют запросу. ≈сли существует подход€щий дл€ запроса индекс, MongoDB может использовать индекс, чтобы ограничить количество документов, которые требуетс€ просмотреть. MongoDB также может возвращать отсортированные результаты запроса, использу€ упор€дочивание в индексе.

¬ MongoDB индекс определ€етс€ на уровне коллекции.

„тобы создать индекс, следует использовать команду db.collection.createIndex():
db.collection.createIndex( <key and index type specification>, <options> )

√еопространственный индекс
„тобы обеспечить эффективное выполннеие запросов по координатам, в MongoDB существует 2 специальных индекса: 2d индекс, который использует  плоскостную геометрию,  и индекс 2dsphere, использующий сферическую геометрию.

—оздание индекса 2dsphere 
ƒл€ создани€ индекса 2dsphere  следует использовать следующую команду
db.collection.createIndex( { <location field> : "2dsphere" } )
где <location field> - это поле, значением которого €вл€етс€ либо объект GeoJSON, либо пара координат.
